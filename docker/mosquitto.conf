# Development broker. Open listener, no authentication — this is a laptop
# demo on a private compose network, not a deployment. Put a password file and
# TLS in front of it before any device you care about connects.

listener 1883 0.0.0.0
allow_anonymous true

persistence false
log_dest stdout
log_type notice
