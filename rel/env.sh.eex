export RELEASE_DISTRIBUTION=sname
export RELEASE_NODE="goatmire@localhost"
# Mix generates a private cookie in releases/COOKIE. Restrict distribution
# listeners to loopback; remote access attaches through the local release CLI.
export ERL_AFLAGS="${ERL_AFLAGS:-} -kernel inet_dist_use_interface '{127,0,0,1}'"
